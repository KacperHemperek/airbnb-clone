export interface ExploreDataType {
    img: string;
    location: string;
    distance: string;
};

export interface AnywhereDateType {
    img: string;
    title: string;
}

export interface SearchResult {
    img: string;
    title: string;
    location: string;
    description: string;
    star: number;
    price: string;
    total: string;
    long: number;
    lat: number;
}